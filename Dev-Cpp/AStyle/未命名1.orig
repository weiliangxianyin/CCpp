#include <stdio.h>

long long fib(int n);
int main(void) {
	int n;
	scanf("%d",&n);
	printf("fib(%d)=%lld",n,fib(n));
	return 0;
}
/*提交以下代码*/

long long fib(int n) {
	if(n==1||n==2)return 1;
	else {
			fib(n)=fib(n-1)+fib(n-2); 
	} 
	return fib(n);
}
